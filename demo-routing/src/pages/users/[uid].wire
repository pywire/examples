print(f"users/{uid}.wire")
print("hello world")

---html---
<article>
    <h3>User Profile</h3>
    <p>Profile ID: <mark>{uid}</mark></p>
    <p>This route is handled by <code>users/[uid].wire</code>.</p>
</article>
