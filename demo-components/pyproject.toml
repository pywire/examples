[project]
name = "demo-components"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "pywire",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
